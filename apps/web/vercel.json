{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "*": false,
      "main": true
    }
  },
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then exit 0; fi; parents=$(git show -s --format=%P \"$VERCEL_GIT_COMMIT_SHA\" | wc -w | tr -d ' '); if [ \"$parents\" -lt 2 ]; then exit 0; fi; exit 1"
}
