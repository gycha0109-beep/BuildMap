name: PIE Prospective Shadow

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
      - converted_to_draft
      - edited

permissions:
  contents: read
  pull-requests: read

jobs:
  pie:
    uses: gycha0109-beep/Project_Intelligence_Engine/.github/workflows/prospective-pr.yml@96f9061d7538e92dbb8fdc8567147b3d6c01744e
    with:
      repository: ${{ github.repository }}
      pull_request_number: ${{ github.event.pull_request.number }}
      event_head_sha: ${{ github.event.pull_request.head.sha }}
      pie_revision: 96f9061d7538e92dbb8fdc8567147b3d6c01744e
      profile: .review/project.yml
      config: .review/intelligence/config.yml
      operational_policy: .review/operational/policy.yml
