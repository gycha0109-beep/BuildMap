name: PIE Prospective Shadow

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
      - converted_to_draft
      - edited

permissions:
  contents: read
  pull-requests: read

jobs:
  pie:
    uses: gycha0109-beep/Project_Intelligence_Engine/.github/workflows/prospective-pr.yml@e09b937f27b93c8c4a65452d3a175c6356367fdc
    with:
      repository: ${{ github.repository }}
      pull_request_number: ${{ github.event.pull_request.number }}
      event_head_sha: ${{ github.event.pull_request.head.sha }}
      pie_revision: e09b937f27b93c8c4a65452d3a175c6356367fdc
      profile: .review/project.yml
      config: .review/intelligence/config.yml
      operational_policy: .review/operational/policy.yml
